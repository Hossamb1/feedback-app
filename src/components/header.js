import React from "react";

const header = () => {
  return (
    <header style={{ backgroundColor: "rgba(0,0,0,0.4)", color: "pink" }}>
      <div className="container">
        <h2>Feedback UI</h2>
      </div>
    </header>
  );
};

export default header;
